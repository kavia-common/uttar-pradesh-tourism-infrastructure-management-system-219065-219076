(function (window) {
  window.__env = window.__env || {};
  // Override API base URL at runtime by editing this file in deployments if needed
  window.__env.API_BASE_URL = window.__env.API_BASE_URL || 'http://localhost:3001';
}(this));
